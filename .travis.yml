language: node_js
node_js:
  - 'stable'
cache:
  directories:
    - node_modules
script:
  - yarn test --watchAll=false
on:
  branch: main
deploy:
  branch: main
  provider: heroku
  api_key: $HEROKU_KEY
  app: $HEROKU_APP
